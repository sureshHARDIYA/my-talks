import { leroyTheme } from "@/theme/leroy";

const leroySpectacleTheme = {
  fonts: {
    header:
      '"Inter", system-ui, -apple-system, "Segoe UI", Roboto, Helvetica, Arial, sans-serif',
    text:
      '"Inter", system-ui, -apple-system, "Segoe UI", Roboto, Helvetica, Arial, sans-serif',
  },
  colors: {
    secondary: leroyTheme.black,
  },
};

export default leroySpectacleTheme;

